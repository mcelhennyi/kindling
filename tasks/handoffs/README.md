# Handoffs

Drop dated markdown handoffs here when context must transfer between people or agent sessions (for example after a parallel batch or a blocked investigation).

Naming suggestion: `YYYY-MM-DD-<topic>.md`.
