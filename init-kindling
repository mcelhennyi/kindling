#!/usr/bin/env bash
# Wrapper: init-kindling — two-step plugin repo setup (skeleton + kindling).
# Run from the plugin repo root after setting origin and making an initial commit.
# See INIT.MD for the full walkthrough.
set -euo pipefail
exec bash "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/scripts/init-kindling.sh" "$@"
# Supports: --template python|react  --slug <kebab-slug>
