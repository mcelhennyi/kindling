[project]
name = "kindling"
version = "0.2.0"
description = "Hearth plugin template layer and Python SDK"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["scripts/tests"]
pythonpath = ["."]
